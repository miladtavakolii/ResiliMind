[project]
name = "resilimind"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Milad Tavakoli", email = "milad.tavakoli81@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "langchain-community>=0.4.2",
    "langchain-ollama>=1.1.0",
    "langgraph>=1.2.11",
    "networkx>=3.6.1",
    "pydantic>=2.13.4",
]

[project.scripts]
resilimind = "resilimind:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
